// 품목코드 자동완성 사전 — 실제 상세명세서(priced_index)에서 10회 이상 쓰인 자재/공정 코드.
// 거래처·브랜드·SKU 성 코드는 제외(공개 repo 유출 방지). 빈도순 정렬(추천 우선순위에 사용).
// 정규화 표준형으로 보관 — 입력이 변형(아크릴3t / 3T아크릴)이면 "혹시 …?" 로 표준형 제안.
export const ITEM_CODES: string[] = [
  '500-2', '아크릴5T', '200', '아크릴', '도장비', '아크릴3T', '타카잔넬', '갈바에폭시', '아크릴8T', '아크릴10T',
  '일체형잔넬', '700', '400', '00001', '스카시', '스카시20T', '0008', '100-0005', '갈바측광', '스카시10T',
  '스카시30T', '포맥스5T', '갈바후광', '100-0004', '아크릴네온', '아크릴2T', '100-0003', '완조립', '100-0001',
  '300', '100-0002', '아크릴측광', '고무스카시10T', '고무스카시20T', '고무스카시30T', '포맥스3T', '0001-5',
  '스카시50T', '스텐', '실리콘네온', '갈바비조명', '14-1', '포맥스10T', '투명아크릴5T', '000', '포맥스8T',
  '넘버원단면', '00003', '갈바오사이', '용접잔넬', '갈바후광채널', '에칭(엠보)시트', '아크릴잔넬', '갈바후렘',
  '골드스텐', '스텐1.2T', '스텐3T', '갈바1.2T', '스텐비조명', '스텐헤어라인', '고무스카시50T', '아크릴20T',
  '100-2', '골드스텐후광', '갈바2T', '일체형에폭시', '일체형전후광', '포맥스2T', '100-006', '200-1',
  '골드스텐도시락', '아크릴15T', '0001-3', '300-1', '갈바전후광', '투명아크릴3T', '아크릴조각사인', '스텐후광',
  '금경아크릴5T', '갈바도시락', '아크릴8T도장', '0000', '갈바절곡', '스텐오사이', '아크릴전측광', '갈바전광',
  '200갈바오사이', '투아크릴3T', '스텐측광', '아크릴투5T', '검포맥스5T', '넘버원양면', '포맥스백5T', '에칭시트',
  '스텐에폭시', '103', '철판3T', '투명아크릴10T', '플랙시네온', '000-1', '검정아크릴5T', '스카시5T',
  '갈바오사이잔넬', '금색스카시20T', '포맥스', '200완조립', '갈바3T', '금색스카시10T', '아크릴5T+3T', '철판2T',
  '골드스텐1.2T', '금색스카시30T', '쓰리아크릴8T', '투명아크릴8T', '14-2', '0000-20', '스텐오사이잔넬',
  '0000-21', '골드스텐비조명', '스텐2T', '포맥스검5T', '스텐(HL)', '200도시락', '골드스텐측광', '갈바레이저타공',
  '스텐절곡', '14-04', '노출검정', '갈바1.6T', '갈바', '스텐5T', '200갈바후광', '타카잔넬행잉', '포맥스백3T',
  '00-2', '아크릴투3T', '시트컷팅', '무광아크릴5T', '골드스텐절곡', '갈바후광잔넬', '아크릴조각', '투명아크릴2T',
  '백색아크릴5T', '아크릴측광채널', '스텐포리싱', '갈바측광채널', '아크릴8T3D', '스텐후광채널', '스텐HL1.2T',
  '갈바먹통', '701', '아크릴10T+5T', '14-3', '일체형전광', '시트커팅', '일반형RGB', '아크릴8T+5T', '스텐후광잔넬',
  '금경아크릴3T', '00000-17', '0002', '0000-18', '검포맥스10T', '갈바오사이채널', '갈바레이저', '아크릴금경5T',
  '0001-2', '스텐(HL)2T', '후광잔넬', '갈바측광잔넬', '아크릴10T도장', '쓰리아크릴10T', '투명3T', '검정포맥스5T',
  '아크릴검5T', '200전후광', '투명5T', '검정반광', '타카잔넬80mm', '200까치발', 'LED-0001', '알마이트2T',
  '포맥스20T', '스텐헤어라인1.2T', '스텐후렘', '포맥스백8T', '은경아크릴5T', '아크릴3T+2T', '스카시금10T',
  '일체형전후광도장', '갈바캡잔넬', '골드스텐포리싱', '0000-51', '00000-50', '갈바도시락방식', '백포맥스5T',
  '투명아3T', '아크릴5T+5T', '검정아크릴3T', '아크릴투8T', '스텐HL3T', '200도장2도', '고무스카시5T', '스카시금30T',
  '철판5T', '아크릴3T+5T', '아크릴검3T', '백색포맥스5T', '적색아크릴5T', '스리아크릴10T', '스텐오사이도시락',
  '스텐1.2T+아3T', '검포맥스3T', '갈바오사이도시락', '골드스텐HL', '0000-19', '00001-01', '100갈바비조명잔넬',
  '투명PC2T',
];

/** 매칭/비교용 정규화 — 공백·슬래시 제거 + 라틴 대문자(5t→5T, hl→HL). */
export function normCode(s: string): string {
  return (s || '').trim().replace(/[\s/]/g, '').toUpperCase();
}

const NORMED = ITEM_CODES.map(normCode);
const SORTED = NORMED.map((c) => [...c].sort().join(''));

/**
 * 자재 그룹·두께 정렬 키 — 같은 자재끼리 묶고(두께 뺀 base) 두께(2T<3T<5T…, 1.2T 등 소수 포함)
 * 오름차순으로 보이게. 두께 없는 코드는 그 그룹 맨 끝(Infinity).
 */
function codeKey(norm: string): { base: string; tk: number } {
  const m = norm.match(/(\d+(?:\.\d+)?)T/); // 첫 두께(예: 3T, 1.2T, 10T)
  return {
    tk: m ? parseFloat(m[1]) : Number.POSITIVE_INFINITY,
    base: norm.replace(/(\d+(?:\.\d+)?)T/, ''), // 두께 제거 = 자재 base(그룹)
  };
}
const KEYS = NORMED.map(codeKey);

/** 입력과 비슷한 품목코드를 빈도·일치도 순으로. (정확>접두>부분>글자재배열) */
export function matchCodes(query: string): string[] {
  const q = normCode(query);
  if (!q) return [];
  const qs = [...q].sort().join('');
  const out: { code: string; s: number; i: number }[] = [];
  for (let i = 0; i < ITEM_CODES.length; i++) {
    const code = NORMED[i];
    let s = 0;
    if (code === q) s = 100;
    else if (code.startsWith(q)) s = 80;
    else if (code.includes(q)) s = 60;
    else if (SORTED[i] === qs) s = 50; // 같은 글자 재배열(3T아크릴 ↔ 아크릴3T)
    if (s) out.push({ code: ITEM_CODES[i], s, i });
  }
  // 일치도(정확>접두>부분>재배열) 우선, 같은 점수 안에서는 자재 base 로 묶고 두께 오름차순.
  out.sort((a, b) => {
    if (b.s !== a.s) return b.s - a.s;
    const ka = KEYS[a.i];
    const kb = KEYS[b.i];
    if (ka.base !== kb.base) return ka.base.localeCompare(kb.base, 'ko');
    if (ka.tk !== kb.tk) return ka.tk - kb.tk;
    return a.i - b.i;
  });
  return out.map((x) => x.code);
}

/** 입력이 어떤 표준코드의 변형(케이스/포맷/순서만 다름)이면 그 표준코드를, 아니면 null. */
export function didYouMean(query: string, matches: string[]): string | null {
  const raw = query.trim();
  const q = normCode(query);
  const qs = [...q].sort().join('');
  for (const code of matches) {
    const nc = normCode(code);
    if (nc === q) return code === raw ? null : code; // 케이스/슬래시만 다름
    if ([...nc].sort().join('') === qs) return code; // 글자 재배열
  }
  return null;
}
